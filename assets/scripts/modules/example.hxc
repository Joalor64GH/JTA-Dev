import jta.modding.module.ScriptedModule;
import jta.modding.module.Module;
import jta.states.MainMenu;
import flixel.FlxG;
import Std;

class ExampleModule extends ScriptedModule
{
	public function new():Void
	{
		super('exampleModule', 1);
	}

	public override function create():Void
	{
		if (Std.isOfType(FlxG.state, MainMenu))
			trace('ExampleModule created');
	}
}
